# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: function-call-openai-sql
services:
    app:
        project: ./app/
        language: py
        host: appservice
hooks:
    postprovision:
        posix:
            shell: sh
            run: ./scripts/create-sq-user..sh
            interactive: true
            continueOnError: false

